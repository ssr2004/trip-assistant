# TravelMind 运行依赖
# 当前开发环境：Python 3.13.7
# 依赖版本使用兼容区间，避免旧固定版本在 Python 3.13 下无法安装。

# Web 框架
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=13.0,<16.0

# AI / LLM / Agent 编排
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langgraph>=1.0,<2.0
openai>=1.50,<3.0

# 配置和数据模型
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
python-dotenv>=1.0,<2.0

# 数据库
SQLAlchemy>=2.0,<3.0

# 向量检索与嵌入模型
faiss-cpu>=1.9,<2.0
sentence-transformers>=3.0,<6.0

# 数据处理和 HTTP 工具
numpy>=2.0,<3.0
pandas>=2.2,<4.0
requests>=2.31,<3.0

# 测试
pytest>=8.0,<10.0
pytest-asyncio>=0.24,<2.0

# 日志
loguru>=0.7,<1.0
